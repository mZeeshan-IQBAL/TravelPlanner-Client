# React App Configuration
# For local development:
REACT_APP_API_URL=http://localhost:5000/api
# For production (replace with your actual Vercel server URL):
# REACT_APP_API_URL=https://your-travel-planner-server.vercel.app/api

# For local development
GENERATE_SOURCEMAP=false

# Disable ESLint warnings in development
ESLINT_NO_DEV_ERRORS=true

# Disable TypeScript type checking for faster builds
TSC_COMPILE_ON_ERROR=true

# Cloudinary Configuration (optional)
# If not provided, the client will fetch cloud name/preset from the server at /api/public-config
REACT_APP_CLOUDINARY_CLOUD_NAME=
REACT_APP_CLOUDINARY_UPLOAD_PRESET=

# Runtime API base handling
# In development the app uses CRA proxy (/api -> http://localhost:5000) unless explicitly forced.
# Set REACT_APP_FORCE_API_URL=true to force using REACT_APP_API_URL even in development.
REACT_APP_FORCE_API_URL=

# Google OAuth (optional)
REACT_APP_GOOGLE_CLIENT_ID=

# Facebook OAuth (optional)
REACT_APP_FACEBOOK_APP_ID=your_facebook_app_id

# Environment
REACT_APP_ENVIRONMENT=development